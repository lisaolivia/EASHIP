<UserControl x:Class="Eaship.page.Controls.AdminNavbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource LightBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= SIDEBAR ================= -->
        <Border Background="{StaticResource NavyBrush}" Grid.Column="0">
            <StackPanel VerticalAlignment="Stretch" Margin="24,40">

                <!-- LOGO -->
                <Button Background="Transparent"
                            BorderThickness="0"
                            HorizontalAlignment="Left"
                            Click="GoDashboard">
                    <Image Source="/Eaship;component/Assets/logo2.png"
                           Width="106" Height="48"
                           RenderTransformOrigin="-0.31,0.508"/>
                </Button>


                <!-- MENU ITEMS -->
                <StackPanel Margin="0,40,0,0">

                    <Button Style="{StaticResource NavButton}"
                            Content="Fleet Management"
                            Margin="0,4"
                            Click="GoFleetManagement"/>

                    <Button Style="{StaticResource NavButton}"
                            Content="Company Verification"
                            Margin="0,4"
                            Click="GoCompanyVerification"/>

                    <Button Style="{StaticResource NavButton}"
                            Content="Booking Request"
                            Margin="0,4"
                            Click="GoBookingRequest"/>

                    <Button Style="{StaticResource NavButton}"
                            Content="Contract Payment"
                            Margin="0,4"
                            Click="GoContractPayment"/>

                    <Button Style="{StaticResource NavButton}"
                            Content="Profile"
                            Margin="0,4"
                            Click="GoProfile"/>
                </StackPanel>

                <!-- LOGOUT -->
                <StackPanel VerticalAlignment="Bottom" Margin="0,40,0,0">
                    <Button Content="Log Out"
                            Background="White"
                            Foreground="{StaticResource NavyBrush}"
                            FontWeight="SemiBold"
                            Padding="10,6"
                            BorderBrush="Transparent"
                            Click="GoLogout"/>
                </StackPanel>

            </StackPanel>
        </Border>

    </Grid>

</UserControl>
